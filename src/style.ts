import styled from 'styled-components'

export const AppStyled = styled.div`
    &.disable-scroll {
        height: 100vh;
        overflow: hidden;
    }
    .main-app-container {
        padding: 0 20px;
        max-width: 980px;
        margin: auto;
    }
`
